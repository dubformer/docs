---
title: 'Create Project'
api: 'POST https://studio.dubformer.ai/api/v1/projects'
description: 'Create a new project with general project information'
---

## Request Body

<ParamField body="name" type="string" required>
  Name of the project.
</ParamField>

<ParamField body="managerEmail" type="string">
  Email of the project manager. If not provided, the API key user will be used as the manager.
</ParamField>

<ParamField body="translationsGlossaryId" type="string">
  Optional ID of a translation glossary to be used for this project.
</ParamField>

<ParamField body="voicesGlossaryId" type="string">
  Optional ID of a voices glossary to be used for this project.
</ParamField>

<ParamField body="stringVariables" type="array">
  Optional array of key-value pairs for storing project metadata. Useful for storing external system references like backlinks, identifiers, or other custom metadata.

  Each item should be an object with `key` and `value` properties (both strings).

  Example:
  ```json
  [
    { "key": "storyTitleId", "value": "12345" },
    { "key": "storyDeeplink", "value": "https://..." }
  ]
  ```
</ParamField>

## Response

<ResponseField name="id" type="string">
  Unique project identifier.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the project.
</ResponseField>

<ResponseField name="status" type="string">
  Current status of the project.
</ResponseField>

<ResponseField name="translationGlossary" type="string | null">
  ID of the translation glossary, if any.
</ResponseField>

<ResponseField name="voicesGlossary" type="string | null">
  ID of the voices glossary, if any.
</ResponseField>

<ResponseField name="remark" type="string | null">
  Project remarks, if any.
</ResponseField>

<ResponseField name="clientName" type="string | null">
  Client name, if specified.
</ResponseField>

<ResponseField name="projectUrl" type="string">
  URL to access the project in the web app.
</ResponseField>

<ResponseField name="createdAt" type="string">
  ISO 8601 timestamp when the project was created.
</ResponseField>

<ResponseField name="updatedAt" type="string">
  ISO 8601 timestamp when the project was last updated.
</ResponseField>

<ResponseField name="stringVariables" type="array | null">
  Array of key-value pairs containing project metadata, if provided.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  https://studio.dubformer.ai/api/v1/projects \
  -H 'Authorization: Bearer YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{
    "name": "Marketing Campaign 2023",
    "managerEmail": "manager@company.com",
    "translationsGlossaryId": "gloss_5f83a14b2ec1b",
    "voicesGlossaryId": "voices_7c92e3a1f56d8",
    "stringVariables": [
      { "key": "storyTitleId", "value": "12345" },
      { "key": "storyItemCode", "value": "PYR_3345" },
      { "key": "storyDeeplink", "value": "https://pyramid.dalet.euronews.tech?titleId=12345&kanbanViewId=7KV6cv" }
    ]
  }'
```

```typescript TypeScript
import axios from 'axios';

const response = await axios.post(
  'https://studio.dubformer.ai/api/v1/projects',
  {
    name: "Marketing Campaign 2023",
    managerEmail: "manager@company.com",
    translationsGlossaryId: "gloss_5f83a14b2ec1b",
    voicesGlossaryId: "voices_7c92e3a1f56d8",
    stringVariables: [
      { key: "storyTitleId", value: "12345" },
      { key: "storyItemCode", value: "PYR_3345" },
      { key: "storyDeeplink", value: "https://pyramid.dalet.euronews.tech?titleId=12345&kanbanViewId=7KV6cv" }
    ]
  },
  {
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    }
  }
);

console.log(response.data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "id": "proj_5f83a14b2ec1b",
  "name": "Marketing Campaign 2023",
  "status": "new",
  "translationGlossary": "gloss_5f83a14b2ec1b",
  "voicesGlossary": "voices_7c92e3a1f56d8",
  "remark": "Created via Dubformer Studio API",
  "clientName": null,
  "projectUrl": "https://studio.dubformer.ai/projects/proj_5f83a14b2ec1b",
  "createdAt": "2023-12-01T10:00:00.000Z",
  "updatedAt": "2023-12-01T10:00:00.000Z",
  "stringVariables": [
    { "key": "storyTitleId", "value": "12345" },
    { "key": "storyItemCode", "value": "PYR_3345" },
    { "key": "storyDeeplink", "value": "https://pyramid.dalet.euronews.tech?titleId=12345&kanbanViewId=7KV6cv" }
  ]
}
```
</ResponseExample>

## Notes

<Note>
  This endpoint creates a project with basic information only. Videos can be added to the project using separate video management endpoints.
</Note>

<Note>
  The project manager must belong to the same company as the API key user. If no manager email is provided, the API key user will be assigned as the project manager.
</Note>

<Note>
  Translation and voices glossaries are optional but must exist and be accessible to your organization if provided.
</Note>

<Note>
  The `stringVariables` field is useful for storing external system references and metadata. Common use cases include storing backlinks to external project management systems (e.g., Dalet, Jira), content identifiers, or any other custom key-value metadata needed for integration purposes.
</Note> 